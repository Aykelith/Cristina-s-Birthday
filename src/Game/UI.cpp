#include <Game/UI.hpp>

#include <SFML/Graphics/RenderTarget.hpp>

namespace XAL {
    void UI::load(const std::string& configurationFile, const sf::Vector2u& windowSize) {

    }

    void UI::draw(sf::RenderTarget& target, sf::RenderStates states) const {

    }
}
